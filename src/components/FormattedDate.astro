---
interface Props {
	date?: Date;
}

const { date } = Astro.props;
---

{date && (
	<time datetime={date.toISOString()}>
		{
			date.toLocaleDateString('es-co', {
				year: 'numeric',
				month: 'short',
				day: 'numeric',
			})
		}
	</time>
)}